class Solution {
    public int isPrefixOfWord(String sentence, String searchWord) {
        String[] s=sentence.split(" ");
        for(int i=0;i<s.length;i++){
            if(s[i].startsWith(searchWord)){
                return i;
            }
        }
        return -1;

        
    }
}
function s(arr){
    var start=0;
    var right=arr.length-1;
    while(start<right){
        var temp=arr[satt];
        arr[let]=arr[right];
        arr[right]=temp;
    }
    let arr=[1,2,3,4];
    let ans=arr.reduce(function(accumulato,val){
        return accumultor+val;

    })
    let arr=1,2,4
    let ans =arr.filter(funtion(val){
        if(val > 4)
    })

}